package com.example.haptok.theme

import androidx.compose.ui.graphics.Color

// Primary brand colors - electric blue/violet gradient palette
val HaptokBlue = Color(0xFF4FC3F7)        // Bright electric blue
val HaptokViolet = Color(0xFF7C4DFF)      // Deep violet
val HaptokPurple = Color(0xFFB388FF)      // Light purple accent
val HaptokCyan = Color(0xFF18FFFF)        // Cyan accent
val HaptokMagenta = Color(0xFFFF4081)     // Magenta for alerts/active

// Surface colors - rich dark
val DarkBackground = Color(0xFF0A0A0F)    // Near-black with blue tint
val DarkSurface = Color(0xFF12121A)       // Slightly lighter surface
val DarkSurfaceVariant = Color(0xFF1A1A2E)// Card backgrounds
val DarkSurfaceElevated = Color(0xFF1E1E32) // Elevated surfaces

// Glass effect colors
val GlassWhite = Color(0x1AFFFFFF)        // 10% white for glassmorphism
val GlassBorder = Color(0x33FFFFFF)       // 20% white for glass borders
val GlassHighlight = Color(0x0DFFFFFF)    // 5% white for subtle highlight

// Text colors
val TextPrimary = Color(0xFFE8E8F0)       // High emphasis text
val TextSecondary = Color(0xFFA0A0B8)     // Medium emphasis
val TextTertiary = Color(0xFF6C6C80)      // Low emphasis

// Status colors
val StatusProcessing = Color(0xFFFFB74D)  // Amber for processing
val StatusComplete = Color(0xFF66BB6A)    // Green for complete
val StatusError = Color(0xFFEF5350)       // Red for error

// Haptic visualization colors
val HapticImpact = Color(0xFFFF5252)      // Red for impacts
val HapticAmbient = Color(0xFF448AFF)     // Blue for ambient
val HapticTexture = Color(0xFF69F0AE)     // Green for texture
val HapticMotion = Color(0xFFFFD740)      // Yellow for motion
